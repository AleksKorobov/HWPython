 
 
13. Списки - операторы и функции работы с ними  
 Функция list() для создания списков. Обращение к отдельным
  элементам списка по индексу. Функции len(), max(), min(), 
  sum(), sorted(). Операторы +, *, in, del.